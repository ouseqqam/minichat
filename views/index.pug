extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages
    p #{message.user} = #{message.text}
  .button-container
   a(href=`https://myminichat.herokuapp.com/new`): button.compose= "New message"
